import 'package:flutter/material.dart';

class SarhProgressBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child:
      CircularProgressIndicator(
        backgroundColor: Color(0xff21eae3),
      ),
    );
  }
}
